# Example .env file for fastapi-energy-platform
# Copy this file to .env and modify as needed.
# These variables will override the default settings in app/config.py

# --- Application Settings ---
# APP_NAME="My Custom Energy Platform"
# APP_VERSION="1.0.0-custom"
# APP_DESCRIPTION="A customized instance of the FastAPI Energy Platform."
# API_V1_PREFIX="/api/custom/v1"

# --- Logging Configuration ---
# Valid levels: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL="INFO"

# --- File Storage Paths ---
# IMPORTANT: For production, set PROJECT_DATA_ROOT to an ABSOLUTE path
# where the application has read/write permissions.
# Example for Linux/macOS:
# PROJECT_DATA_ROOT="/var/data/energy_platform_projects"
# Example for Windows:
# PROJECT_DATA_ROOT="D:/data/energy_platform_projects"
# If not set, it defaults to a 'data/projects' directory relative to the repository root (for development).
PROJECT_DATA_ROOT=

# --- CORS (Cross-Origin Resource Sharing) ---
# Comma-separated list of allowed origins.
# Example: ALLOWED_ORIGINS="http://localhost:8080,https://myfrontend.com"
# If not set, defaults to: "http://localhost,http://localhost:3000,http://localhost:5173"
ALLOWED_ORIGINS=

# --- Example Service-Specific Setting ---
# DEFAULT_FORECAST_TARGET_YEAR=2030
