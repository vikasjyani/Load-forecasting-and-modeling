import React from 'react';

const LoadingSpinner = () => {
  return (
    <div className="loading-spinner">
      <p>Loading...</p>
      {/* Add a more sophisticated spinner animation here if desired */}
    </div>
  );
};

export default LoadingSpinner;
